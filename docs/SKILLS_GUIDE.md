# Skills 开发指南

本文档介绍如何创建和使用 Skills（技能），让 Claude 根据预定义的任务模板自动执行特定领域的任务。

---

## 什么是 Skill？

**Skill** 是一个预定义的任务模板，包含：
- 执行任务所需的上下文和指令
- 允许使用的工具列表
- 任务的元数据（名称、描述、标签等）

当用户提问时，Claude Code CLI 会：
1. 自动扫描 `.claude/skills/` 目录
2. 根据用户意图匹配相关的 Skill
3. 将 Skill 内容注入到对话中
4. 按照 Skill 定义的流程执行任务

---

## 目录结构

Skills 存放在项目的 `.claude/skills/` 目录下：

```
your-project/
├── .claude/
│   └── skills/
│       ├── investment-memo/
│       │   └── SKILL.md
│       ├── code-review/
│       │   └── SKILL.md
│       └── api-design/
│           └── SKILL.md
├── src/
└── ...
```

每个 Skill 是一个独立的目录，包含一个 `SKILL.md` 文件。

---

## SKILL.md 格式

SKILL.md 使用 **Frontmatter + Markdown** 格式：

```markdown
---
name: investment-memo
description: 撰写深度股票投资分析报告。当用户要求股票分析、投资建议、研报时使用。
allowed-tools: get_stock_data, search_market_news, WebSearch
version: 1.0.0
tags:
  - finance
  - analysis
---

# 投资研报撰写标准

你是一名专业的投资分析师。请严格按照以下流程执行分析任务。

## 可用工具

| 工具 | 用途 | 参数 |
|------|------|------|
| `get_stock_data` | 获取股票实时数据 | ticker (股票代码) |
| `search_market_news` | 搜索市场新闻 | query (搜索词) |
| `WebSearch` | 网络搜索 | query (搜索词) |

## 分析流程

1. **数据收集**
   - 调用 `get_stock_data` 获取基础数据
   - 调用 `search_market_news` 获取最新新闻

2. **基本面分析**
   - 分析财务指标
   - 评估估值水平

3. **风险评估**
   - 识别主要风险
   - 给出风险等级

4. **投资建议**
   - 给出买入/持有/卖出建议
   - 说明理由

## 输出格式

请使用以下 Markdown 格式输出：

```markdown
# 【深度分析】{公司名称} ({股票代码})

## 📊 核心数据
...

## 📈 基本面分析
...

## ⚠️ 风险提示
...

## 💡 投资建议
...
```

## 注意事项

- 数据必须来自工具调用，不要编造
- 分析要客观、有据可查
- 风险提示要全面
```

---

## Frontmatter 字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `name` | string | ✅ | Skill 唯一标识，小写+连字符，如 `investment-memo` |
| `description` | string | ✅ | **触发条件描述**（非常重要！CLI 根据此匹配用户意图） |
| `allowed-tools` | string/array | ❌ | 允许使用的工具列表，逗号分隔或数组 |
| `version` | string | ❌ | 版本号 |
| `author` | string | ❌ | 作者 |
| `tags` | array | ❌ | 标签，用于分类和搜索 |

### description 的重要性

`description` 是 CLI 用来判断是否使用该 Skill 的关键字段。应该包含：

1. **Skill 的功能描述** - 这个 Skill 做什么
2. **触发条件** - 什么情况下应该使用这个 Skill

**好的 description 示例：**
```yaml
description: 撰写深度股票投资分析报告。当用户要求股票分析、投资建议、研报、行情分析、估值评估时使用。
```

**不好的 description 示例：**
```yaml
description: 投资分析  # 太简短，缺少触发条件
```

---

## 使用方式

### 方式 1：CLI 自动发现

只需在包含 `.claude/skills/` 目录的项目中运行 `claude`：

```bash
cd /path/to/your-project
claude
```

然后正常对话，CLI 会自动匹配相关 Skill：

```
> 帮我分析一下苹果股票
📚 使用 Skill: investment-memo
🔧 调用: get_stock_data(AAPL)
🔧 调用: search_market_news(apple)
...
```

### 方式 2：SDK 指定工作目录

```python
from claude_agent_sdk import query, ClaudeAgentOptions

options = ClaudeAgentOptions(
    cwd="/path/to/your-project"  # CLI 会自动发现 .claude/skills/
)

async for message in query(
    prompt="帮我分析一下苹果股票",
    options=options
):
    print(message)
```

### 方式 3：显式调用 Skill

使用 `/skill-name` 语法：

```
> /investment-memo 分析微软股票
```

或使用 Skill 工具：

```python
# SDK 中
options = ClaudeAgentOptions(
    allowed_tools=["Skill"]
)
```

---

## 创建 Skill 的步骤

### 1. 创建目录

```bash
mkdir -p .claude/skills/my-skill
```

### 2. 创建 SKILL.md

```bash
cat > .claude/skills/my-skill/SKILL.md << 'EOF'
---
name: my-skill
description: 我的技能描述。当用户要求xxx时使用。
allowed-tools: Read, Write
---

# 我的技能

## 任务说明
...

## 执行流程
1. ...
2. ...

## 输出格式
...
EOF
```

### 3. 测试

```bash
cd /path/to/project
claude
> 执行 my-skill 相关的任务
```

---

## 最佳实践

### 1. description 要精确

描述应该清晰说明：
- 这个 Skill 做什么
- 什么情况下触发

```yaml
# ✅ 好
description: 代码审查助手。当用户要求代码审查、review、检查代码质量时使用。

# ❌ 差
description: 代码相关
```

### 2. 工具列表最小化

只列出真正需要的工具：

```yaml
# ✅ 好 - 只需要读取代码
allowed-tools: Read, Grep

# ❌ 差 - 给了不必要的权限
allowed-tools: Read, Write, Bash, WebFetch, WebSearch
```

### 3. 流程要具体可操作

```markdown
## 执行流程

1. **收集信息**
   - 使用 `Read` 读取代码文件
   - 使用 `Grep` 搜索相关引用

2. **分析问题**
   - 检查代码规范
   - 识别潜在 bug
   - 评估性能问题

3. **输出报告**
   - 按严重程度分类
   - 给出修复建议
```

### 4. 提供输出格式模板

```markdown
## 输出格式

请使用以下格式：

# 代码审查报告

## 🔴 严重问题
- ...

## 🟡 建议改进
- ...

## ✅ 优点
- ...
```

---

## 示例 Skills

### 代码审查

```yaml
---
name: code-review
description: 代码审查助手。当用户要求代码审查、review、检查代码时使用。
allowed-tools: Read, Grep, Glob
---
```

### API 设计

```yaml
---
name: api-design
description: API 设计助手。当用户要求设计 API、接口规范、RESTful 设计时使用。
allowed-tools: Read, Write, WebSearch
---
```

### 文档生成

```yaml
---
name: doc-generator
description: 文档生成助手。当用户要求生成文档、README、API 文档时使用。
allowed-tools: Read, Write, Glob
---
```

---

## 与 MCP 工具配合

Skills 可以使用 MCP 工具。在 `allowed-tools` 中使用完整的工具名：

```yaml
---
name: investment-memo
allowed-tools: mcp__finance-tools__get_stock_data, mcp__finance-tools__search_market_news
---
```

确保 MCP 服务器已在 `.mcp.json` 中配置：

```json
{
  "mcpServers": {
    "finance-tools": {
      "command": "python",
      "args": ["mcp_servers/finance.py"]
    }
  }
}
```

---

## 故障排除

### Skill 没有被识别

1. 检查目录结构是否正确：`.claude/skills/skill-name/SKILL.md`
2. 检查 Frontmatter 格式是否正确（`---` 分隔）
3. 确保 `name` 和 `description` 字段存在

### Skill 没有被匹配

1. 检查 `description` 是否包含触发条件关键词
2. 尝试使用 `/skill-name` 显式调用

### 工具调用失败

1. 检查工具名是否正确
2. 检查 MCP 服务器是否运行
3. 检查权限设置

---

## 参考链接

- [Claude Code 官方文档](https://docs.anthropic.com/en/docs/claude-code)
- [工具参考文档](./TOOLS.md)
- [示例项目](https://github.com/anthropics/claude-agent-sdk-python/tree/main/examples)
